import { route_config_mapped } from '@harmony/config/route_config'

const config = {
  id: 'flow',
  kind: 'Flow',
  color: '#57A070',
  icon: 'elf',
  landing: [route_config_mapped.scan],
}

export default config
